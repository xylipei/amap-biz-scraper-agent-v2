# 产品需求文档（PRD）：高德地图区域商家信息抓取Agent

**文档版本**：V1.1  
**更新日期**：2026-07-18  
**编写目的**：指导AI智能体开发，明确功能边界与工程底线，防止VibeCoding过程中出现逻辑降级、安全漏洞与异常崩溃。

## 1. 项目概述

### 1.1 项目背景与目标
本项目旨在开发一个基于大模型的智能体，通过自然语言交互，自动化抓取高德地图指定区域、指定品类商家的结构化信息。目标是将繁琐的API调用、数据清洗与聚合分析交由Agent自动完成，**最终交付为结构化数据对象及可直接使用的表格文件（CSV/Excel）**。

### 1.2 VibeCoding 防跑偏原则
*   **定位为“起步方式”而非“上线方式”**：AI生成的代码必须经过边界测试后方可合并。
*   **拒绝擅自降级**：当遇到复杂数据（如团购抓取、复合关键词）时，AI不得擅自降低难度或删减需求，必须严格按照预设的逻辑分支执行。
*   **守住安全底线**：严禁将高德API Key硬编码在代码中，必须通过环境变量读取；严控请求频率，防止账号被封。

## 2. 核心功能需求

### 2.1 输入定义
用户通过自然语言输入指令，Agent需能准确提取以下核心参数：
1.  **目标区域**：如“北京市海淀区”、“上海闵行区”或特定的经纬度多边形。
2.  **目标品类**：如“咖啡厅”、“星巴克”、“水果超市”。
3.  **数据规模预期**：如“前50家”或“全部”。

### 2.2 交付目标与输出定义
Agent执行完毕后，**必须同时交付内存中的结构化数据和本地表格文件**。

1.  **结构化数据对象**：供下游系统或大模型进一步解析的JSON/字典列表。
2.  **表格文件（强制要求）**：必须将结构化数据导出为CSV或Excel（.xlsx）格式，并保存在本地。文件名需包含区域与品类信息（如：`上海闵行区_水果店_20260718.csv`）。程序运行结束时，需在控制台明确提示文件保存的绝对路径。

**数据字段规范**：

| 字段名 | 说明 | 数据获取策略与边界定义 |
| :--- | :--- | :--- |
| **门店名称** | 商家在高德的注册名称 | 直接由高德API返回 |
| **地址** | 商家详细地址 | 直接由高德API返回 |
| **同名门店数量** | 该区域内该品牌/同名商家的总数 | Agent内部对API返回的数据进行聚合计数得出 |
| **高德收录年份** | 商家被高德地图收录的初始年份 | **不可行字段**：高德API不提供此数据，Agent须直接返回“N/A”，禁止伪造或猜测时间戳。 |
| **是否支持团购** | 商家是否有团购/优惠活动 | **受限字段**：优先调用高德详情页接口解析；若遇反爬限制，降级返回该商户高德地图详情页URL，标注为“需人工核验”。 |

## 3. 高频复合输入场景处理策略

针对用户高频使用的“区域/品类”或“区域/品类+服务属性”输入模式，Agent的意图解析模块必须实现以下拆解与路由逻辑：

### 3.1 输入解析与实体分离
当用户输入包含 `/`、空格或自然语言组合时，Agent需将其拆解为三个维度，而非作为一个整体关键词：
*   **区域实体**：如“上海闵行区”
*   **品类实体**：如“水果超市”、“水果店”
*   **服务/意图修饰词**：如“团购”

### 3.2 场景化执行路由

**场景 A：输入为“上海闵行区/水果超市”**
*   **解析**：区域=上海闵行区，品类=水果超市，无特殊服务要求。
*   **执行**：正常调用基础抓取工具，keyword 设为“水果超市”，city/region 设为“闵行区”。输出常规列表并生成表格。

**场景 B：输入为“上海闵行区/水果团购”**
*   **解析**：区域=上海闵行区，隐含品类=水果/水果店，核心诉求=寻找支持团购的商家。
*   **防跑偏策略**：高德POI搜索API不支持直接以“团购”作为有效品类关键词进行搜索，若直接搜“水果团购”会返回空结果或极低质量结果。
*   **执行逻辑**：
    1.  Agent需自动将搜索关键词修正为“水果店”或“水果超市”。
    2.  获取闵行区的基础水果店列表。
    3.  **强制过滤**：在获取团购状态后，Agent必须在生成最终表格前，**剔除没有团购活动的商家**，仅返回并保存支持团购的水果店列表。
    4.  若因反爬原因无法准确获取团购状态，Agent需在表格中将团购字段填为“需人工核验(附链接)”，并向用户声明限制。

## 4. 系统工作流与Agent工具设计

### 4.1 Agent 工具定义
为保证代码结构清晰，AI生成代码时必须按以下模块划分工具函数：

1.  **`fetch_pois(api_key, keyword, region, page_num)`**
    *   **职责**：调用高德 `/place/text` 或 `/place/polygon` 接口。
    *   **防跑偏要求**：必须实现**分页机制**（循环调用直至无数据），必须包含**重试机制**（网络超时重试3次），必须包含**QPS控制**（休眠机制，如每次请求间隔200ms）。
2.  **`parse_groupbuy_info(poi_id, detail_url)`**
    *   **职责**：抓取商户详情页判断是否有团购标签。
    *   **防跑偏要求**：若触发反爬（HTTP 403或IP封禁），不可抛出异常导致程序崩溃，必须捕获异常并返回 `{"groupbuy": "fetch_failed", "url": detail_url}`。
3.  **`aggregate_and_clean(raw_pois_list)`**
    *   **职责**：清洗原始JSON，统计同名门店数量，过滤冗余字段。
    *   **防跑偏要求**：处理空值，确保输出字典结构一致。若上一级触发了“场景B”逻辑，需在此处根据团购字段过滤无效数据。
4.  **`export_to_table(data, filename)`**
    *   **职责**：将结构化数据列表导出为CSV或Excel文件。
    *   **防跑偏要求**：必须处理中文编码问题（CSV需使用 `utf-8-sig`），必须返回文件保存的绝对路径。

### 4.2 Agent 执行流程
1.  **意图解析**：大模型解析用户输入，按第3章策略提取区域、品类、修饰词。若缺失核心参数，主动向用户发起多轮对话追问。
2.  **任务编排**：按顺序调用 `fetch_pois` -> `aggregate_and_clean` -> `parse_groupbuy_info` -> `export_to_table`。
3.  **结果输出**：向用户汇报数据获取统计（如总计抓取X家，成功解析团购Y家，降级处理Z家），并明确提示表格文件的保存路径。

## 5. 非功能需求与工程底线

此部分为 VibeCoding 最易遗漏的工程底线，AI在生成代码时**必须严格遵守**：

### 5.1 安全底线
*   **密钥安全**：API Key 必须通过 `os.getenv('AMAP_API_KEY')` 读取，代码中严禁出现明文密钥。
*   **输入校验**：对用户输入的区域和品类进行基础校验，防止注入风险。

### 5.2 稳定性与容错
*   **API配额防穿透**：在 `fetch_pois` 函数入口处增加配额检查逻辑，若高德API返回配额超限错误码（如 `10003` 或 `10044`），立即终止抓取并提示用户。
*   **异常隔离**：单个门店的团购信息抓取失败，绝对不能影响整体列表数据的输出和表格文件的生成。必须使用 `try-except` 包裹详情抓取逻辑。

### 5.3 日志与可观测性
*   **关键节点日志**：代码必须集成 `logging` 模块，记录：开始抓取时间、API调用URL及响应状态码、反爬触发记录、文件保存路径。
*   **控制台输出**：在终端运行时，需有进度提示（如 `[进度] 正在抓取第 2/10 页...`），避免用户在长时间等待时以为程序卡死。

## 6. 验收标准

1.  **快乐路径验收**：输入“北京海淀区 星巴克”，能正确输出结构化数据，并成功生成包含名称、地址、同名门店数量、是否团购（或链接）的表格文件，控制台提示文件路径。
2.  **复合场景验收**：输入“上海闵行区/水果团购”，程序能自动识别“团购”为修饰词，以“水果店”为关键词搜索，表格中仅包含有团购活动的商家（或明确标注需人工核验）。
3.  **边界验收（防跑偏测试）**：
    *   断开网络运行：程序应输出明确的错误日志并退出，不得抛出未经处理的堆栈报错，不得生成空文件。
    *   模拟API Key失效：程序应提示“API Key无效或配额超限”，不得继续发送无效请求。
    *   模拟高德详情页反爬：程序不得崩溃，必须生成包含基础POI信息的表格文件，并在表格的团购字段标注“需人工核验”及链接。
    *   检查代码文件：全局搜索不得出现明文 API Key。

---
**VibeCoding 启动指令建议**：
“请仔细阅读以上PRD文档。我将使用VibeCoding进行开发，你作为AI程序员，必须严格遵守文档中的《2.2 交付目标》、《3. 高频复合输入场景处理策略》和《5. 非功能需求与工程底线》。不要省略任何异常处理、日志模块和文件导出逻辑，不要擅自简化逻辑。如果理解无误，请先输出你的开发计划和技术栈选型。”