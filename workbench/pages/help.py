"""
使用帮助页 - 分步指南与常见问题
"""

import streamlit as st

st.title(":book: 使用帮助")
st.caption("三步上手，常见问题一页看完。")

st.subheader("📖 使用步骤")
steps = [
    ("1️⃣ 配置 API Key", "「API 设置」页填写高德与 DeepSeek Key。没有 Key 的话：高德去 console.amap.com 申请，DeepSeek 去 platform.deepseek.com 申请。保存后立即生效。"),
    ("2️⃣ 添加中心点", "「中心点管理」页添加或批量导入搜索中心点。每个中心点 = 一个地址 + 一个品类，例如「南京夫子庙 / 咖啡厅」。"),
    ("3️⃣ 执行批量任务", "「批量任务」页勾选中心点创建任务，一键执行。执行中可实时查看进度与日志。"),
    ("4️⃣ 查看与导出", "「结果库」页查看每个中心点的抓取结果；「合并分析」页一键合并去重、按品牌聚合，导出 Excel。"),
]
for title, desc in steps:
    with st.expander(title, expanded=True):
        st.markdown(desc)

st.subheader("🤔 常见问题")
faqs = [
    ("提示「缺少高德地图 API Key」？",
     "还没配置 Key。到「API 设置」页填写后保存即可；也可直接编辑项目根目录的 .env 文件。"),
    ("提示「高德API Key无效 / 配额超限」？",
     "Key 填错了、权限不足或当天配额用完了。检查「API 设置」页填写的 Key，或到高德控制台查看配额。高德免费版每天有调用次数限制。"),
    ("某个中心点搜不到商家？",
     "可能原因：① 地址写法不规范（高德要求具体到区县级，如「南京市鼓楼区」）② 该区域确实没有该品类商家。程序会自动降级扩大范围重试一次。"),
    ("团购状态显示「需人工核验(附链接)」？",
     "高德详情页触发了反爬限制，程序已优雅降级。该商家仍会出现在表格中，可通过链接手动确认。"),
    ("批量任务跑得慢？",
     "每个中心点需要：地理编码 → 周边搜索 → 团购检测（每店间隔 0.5 秒防限流）。中心点越多、商家越多耗时越长，属正常现象。"),
    ("数据存在哪里？",
     "中心点与任务配置存在 workbench_data/（json），抓取结果存在 output/（CSV），日志在 logs/。这些都已被 git 忽略，不会误提交。"),
    ("API Key 安全吗？",
     "Key 只保存在本机 .env 文件中（已加入 .gitignore），不会上传或提交到代码仓库。"),
    ("可以重新跑同一个任务吗？",
     "可以。重复执行同一任务会重置上一轮结果再从头跑，不会重复追加。"),
]
for q, a in faqs:
    with st.expander(q):
        st.markdown(a)

st.subheader("🚀 快速体验")
st.markdown(
    "首页「使用向导」点 **导入示例** 即可获得 3 个南京示例中心点，"
    "随后到「批量任务」创建任务即可体验完整流程（需先配置 API Key）。"
)
